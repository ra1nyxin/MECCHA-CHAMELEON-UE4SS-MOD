enum class ESteamWorkshopMapNetworkLoadState {
    Idle = 0,
    Preparing = 1,
    Downloading = 2,
    Mounting = 3,
    Scanning = 4,
    Loading = 5,
    Loaded = 6,
    Failed = 7,
    ESteamWorkshopMapNetworkLoadState_MAX = 8,
};

enum class ESteamWorkshopMapVisibility {
    Public = 0,
    FriendsOnly = 1,
    Private = 2,
    Unlisted = 3,
    ESteamWorkshopMapVisibility_MAX = 4,
};

